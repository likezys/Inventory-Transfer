# ===================== Python =====================
__pycache__/
# ===================== 虚拟环境 =====================
venv/
.venv/
env/
ENV/
.venv.bak/

# ===================== PyInstaller 构建产物 =====================
build/
dist/
*.spec.bak
# 保留本项目自带的 门店大调拨.spec（打包配置），不忽略

# ===================== 密钥 / 环境变量 =====================
# 切勿将账号密码等敏感信息提交进仓库
.env
.env.*
*.local.env
*.secret

